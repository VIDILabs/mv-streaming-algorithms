import glob
from distutils.core import setup

setup(
    name='prog_causality',
    version=0.1,
    packages=[''],
    package_dir={'': '.'},
    package_data={'': []},
    py_modules=['prog_causality'])
